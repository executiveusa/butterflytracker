# Handoff
Review conservation facts with a local biologist, replace CSS/SVG fallbacks with optimized GLB assets when available, connect Supabase Auth/RLS, and run Playwright after installing @playwright/test.
