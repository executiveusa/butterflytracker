import { useState } from 'react';import { Species, species } from './seed-species';import { SpeciesFilters } from './SpeciesFilters';import { SpeciesGrid } from './SpeciesGrid';import { SpeciesProfileDrawer } from './SpeciesProfileDrawer';import { SiteNav } from '@/components/layout/SiteNav';
export default function AtlasPage(){const [filter,setFilter]=useState('');const [open,setOpen]=useState<Species|null>(null);return <main className="min-h-screen bg-[#EEF3E8] text-[#1E1A16]"><SiteNav/><section className="mx-auto max-w-7xl px-6 pb-20 pt-32"><p className="uppercase tracking-[.3em] text-[#496B3A]">Atlas de México</p><h1 className="mt-4 font-serif text-6xl">Mariposas</h1><p className="mt-4 max-w-3xl text-xl">Explora especies insignia y candidatas registradas para educación. Puerto Vallarta se marca “por verificar” salvo evidencia local validada.</p><div className="my-8"><SpeciesFilters active={filter} setActive={setFilter}/></div><SpeciesGrid filter={filter} onOpen={setOpen}/><SpeciesProfileDrawer s={open} onClose={()=>setOpen(null)}/><p className="mt-8 text-sm">{species.length} fichas con enlaces de fuente y cautela de verificación.</p></section></main>}
