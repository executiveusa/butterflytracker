import { test, expect } from '@playwright/test';test.use({viewport:{width:390,height:844},reducedMotion:'reduce'});test('cinematic home mobile reduced motion',async({page})=>{await page.goto('/');await expect(page.getByText('Monarcas y Morphos').first()).toBeVisible();await expect(page.getByText('Emprende el viaje')).toBeVisible();await page.goto('/mariposas');await expect(page.getByText('Atlas de México')).toBeVisible();await page.goto('/app');await expect(page.getByText('Panel Monarcas y Morphos')).toBeVisible();});
