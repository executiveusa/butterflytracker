# Recovery
Run `npm install`, `npm run build`, then inspect routes `/`, `/mariposas`, `/app`, and `/legal-y-conservacion`. Conservation claims are intentionally cautious and marked permit-gated where necessary.
